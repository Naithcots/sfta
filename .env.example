NODE_ENV = development
HOST = 0.0.0.0
PORT = 8080

# Generate a key by running `openssl rand -hex 64`
# OR
# Paste the key generated from '/keygen' route in development while the server is running
API_KEY = 

WEB_DOMAIN_URL = 